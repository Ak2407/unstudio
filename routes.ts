export const publicRoutes = ["/", "/api/uploadthing", "/api/s3-upload-img"];

export const authRoutes = ["/auth/login", "/auth/register", "/auth/error"];

export const apiAuthPrefix = "/api/auth";

export const DEFAULT_LOGIN_REDIRECT = "/canvas";
